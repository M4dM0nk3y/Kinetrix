# Motor Speed Control
# Gradually increases motor speed using PWM

program {
    make var speed = 0
    
    loop forever {
        set pin 9 to speed
        change speed by 10
        
        make var check = speed % 256
        set speed to check
        
        wait 100
    }
}
