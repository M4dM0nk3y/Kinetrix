# PWM Fade Effect
# Fades LED brightness up and down

program {
    make var brightness = 0
    
    loop forever {
        set pin 9 to brightness
        change brightness by 5
        
        make var check = brightness % 256
        set brightness to check
        
        wait 20
    }
}
